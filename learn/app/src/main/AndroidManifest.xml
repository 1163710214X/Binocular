<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.learn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".FirstActivity"
            android:label="Binocular">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondActivity"
            android:label="Parent " />
        <activity
            android:name=".MessageActivity"
            android:label="Message " />
        <activity
            android:name=".LocationActivity"
            android:label="Location " />
        <activity
            android:name=".HealthActivity"
            android:label="Health " />
        <activity
            android:name=".TeacherActivity"
            android:label="Teacher " />
        <activity
            android:name=".StudentOfTeacherActivity"
            android:label="Student" />
        <activity
            android:name=".MessageFromParentsActivity"
            android:label="From Parents of Students" />
        <activity
            android:name=".DismissalActivity"
            android:label="Dimissal" />
        <activity android:name=".AnalysisActivity"
            android:label="AnalysisActivity"/>
        <activity android:name=".ShowAnalysisActivity"
            android:label="ShowAnalysisActivity"></activity>
    </application>

</manifest>